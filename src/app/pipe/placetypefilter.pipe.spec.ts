import { PlacetypefilterPipe } from './placetypefilter.pipe';

describe('PlacetypefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacetypefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
